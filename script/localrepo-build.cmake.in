add_custom_target(${name})
add_custom_target(${name}-patch ${LOCALREPO_PATCH_COMMAND})
add_custom_target(${name}-configure ${LOCALREPO_CONFIGURE_COMMAND})
add_custom_target(${name}-build ${LOCALREPO_BUILD_COMMAND})
add_custom_target(${name}-install ${LOCALREPO_INSTALL_COMMAND})
add_dependencies(${name} ${name}-install)
add_dependencies(${name}-install ${name}-build)
add_dependencies(${name}-build ${name}-configure)
add_dependencies(${name}-configure ${name}-patch)
